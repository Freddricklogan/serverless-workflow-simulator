{
  "name": "serverless-workflow-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "eslint . && node src/contrast-check.mjs src/exec-shell.css",
    "validate": "html-validate index.html",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "html-validate": "^9.0.0",
    "vitest": "^3.0.0"
  }
}
